#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    int n,m;
    scanf("%d %d", &n, &m);
    ll x[n];
    for(int i=0;i<n;i++){
        cin>>x[i];
    }
    ll y[m];ll res = 0;
    sort(x, x+n);
    int to_alot = m;
    int curr_distance = 1;
    vector<int> ranges;
    for(int i=0;i<n;i++){
        if(i==0){

        }
    }


}